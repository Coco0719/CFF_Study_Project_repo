#pragma once

#include <iostream>
#include <ctime>
#include <cstdlib>

using namespace std;

extern void winNumber(int num[]);
extern void playerNumber(int num[]);
extern int cheakLottoNumber(int player[], int lotto[]);
extern int lottoResultMoney(int equalNum);
extern void printNumber(int arr[], const char *string);
